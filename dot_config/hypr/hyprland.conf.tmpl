# https://wiki.hyprland.org/Configuring/

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
source = ./macchiato.conf
source = ./plugins.conf
source = ./keybinds.conf

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
# Set env first, some of my apps require them, especially for autostart

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

env = SDL_VIDEODRIVER,wayland									# Run SDL2 applications on Wayland. Remove or set to x11 if games that provide older versions of SDL cause compatibility issues
env = GDK_BACKEND,wayland,x11,* 							# GTK: Use wayland if available. If not: try x11, then any other GDK backend.

env = QT_QPA_PLATFORM,wayland;xcb							# Qt: Use wayland if available, fall back to x11 if not.
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1 	# Disables window decorations on Qt applications
env = QT_QPA_PLATFORMTHEME,qt5ct							# Tells Qt based applications to pick your theme from qt5ct, use with Kvantum.

env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

env = TERMINAL,wezterm


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

{{- if eq .chezmoi.hostname "cordyceps" }}
# monitor = eDP-1, 2880x1920@120.00000
{{- end }}
#
#TODO: 120hz !!?
$monwork = DP-1, 3840x2160@60.00, -2560x0, 1.5
$monside = DP-2, 2560x1440@239.972000, 0x0, 1
$monmisc = HDMI-A-2, preferred, 2560x0, 1, transform, 3   # Vertical
# $monhifi = HDMI-A-1, disabled
monitor = HDMI-A-1, 3840x2160@60.00000, -3840x0, 3, vrr, 0

monitor = $monwork
monitor = $monside
monitor = $monmisc
# # monitor = HDMI-A-2, preferred, -1920x0, 1               # Horizontal
# # monitor= HDMI-A-1, preferred, 2560x0, 3



# Found the correct command for the idea - might be worth a retry or little script: hyprctl keyword monitor HDMI-A-1,3840x2160@60,0x0,1.0
# $togglescreens = hyprctl keyword monitor HDMI-A-2, disabled; hyprctl keyword monitor HDMI-A-2, disabled; hyprctl keyword monitor DP-1, disabled; hyprctl keyword monitor $monhifi
# $togglepc = hyprctl keyword monitor $monwork; hyprctl keyword monitor $monmisc; hyprctl keyword monitor $monside; 
###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# Ignore maximize requests from apps.
windowrule = suppressevent maximize, class:.*

# CS2 tweak, https://wiki.hypr.land/Configuring/Tearing/#enabling-tearing
windowrule = immediate, class:^(cs2)$

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$


#################
### AUTOSTART ###
#################

# https://wiki.hypr.land/FAQ/#fullscreen-applicationssteam-games-open-with-secondary-monitors-resolution
# Not sure it works on wayland
exec-once= xrandr --output DP-2 --primary
# Service alternative: systemd fails cause the wayland socket from hyprland isn't ready yet
exec-once= kanshi
# Auth daemon for GUI apps requesting privilege elevation
exec-once = systemctl --user start hyprpolkitagent


#
# Utility
# exec-once = hyprnotify		# Part of ecosystem but non-bundled. For hyprctl notify.
# exec-once = hyprpaper			# Wallpaper
exec-once = hyprsunset  		# Blue light & gamma (brightness) filter # Also see for IPC through hyperctl: https://wiki.hypr.land/Hypr-Ecosystem/hyprsunset/#ipc
exec-once = ags run         # Widgets (Taskbar)
exec-once = hypridle	  		# Needs above env vars to find its own config
# exec-once = mpvpaper --loop ALL ~/Videos/wallpaper/pine-forest-wallpaperwaves-com-ctp-macchiato.mp4
# Didn't fix, 40gb memory!!!
# exec-once = ~/.config/hypr/mpvpaperfix.sh #Wallpaper with memleak fix
exec-once = hyprpm reload   # Hypr plugin manager (Mouse cursor etc.)
# exec-once = ianny					# Reminder utility for taking screen breaks


{{- if eq .chezmoi.hostname "amanita" }}

$mon = DP-1
$ws = 1 
workspace = $ws, monitor:$mon, persistent:true, layoutopt:orientation:top
exec-once = [workspace $ws silent] obsidian
$ws = 2
workspace = $ws, monitor:$mon, persistent:true
# exec-once = [workspace $ws silent] ticktick 
# exec-once = [workspace $ws silent] $terminal #$fileManager
# Doesn't work, at least not with how steam is currently configured
$ws = 3
workspace = $ws, monitor:$mon, persistent:true
# exec-once = [workspace $ws] steam 


$mon = DP-2
$ws = 4
workspace = $ws, monitor:$mon, persistent:true, default:true, name:main
# exec-once = [workspace special:browser] zen-browser
exec-once = [workspace $ws] zen-browser
windowrule= workspace $ws silent, class:^(zen)$
$ws = 5
workspace = $ws, monitor:$mon, persistent:true
# exec-once = [workspace $ws silent] $terminal 
$ws = 6
workspace = $ws, monitor:$mon, persistent:true
# exec-once = [workspace $ws silent] Placeholder


$mon = HDMI-A-2
$ws = 7 
workspace = $ws, monitor:$mon, persistent:true, default:true, layoutopt:orientation:top
windowrule= workspace $ws silent, class:^(ticktick|Proton Mail|KeePassXC|Slack)$
exec-once = [workspace $ws silent] slack & proton-mail & keepassxc & ticktick
exec-once = sleep 4 && hyprctl dispatch resizewindowpixel exact 26% 38%,class:KeePassXC
$ws = 8
workspace = $ws, monitor:$mon, persistent:true, layoutopt:orientation:top
windowrule= workspace $ws silent, class:^(discord|Signal|org.pulseaudio.pavucontrol)$
# attempt to fix pavu race condition with gtk theming
exec-once = sleep 2 && [workspace $ws silent] pavucontrol & discord & signal-desktop
exec-once = hyprctl dispatch resizewindowpixel 100% 20%,class:^(org.pulseaudio.pavucontrol)$
$ws = 9
workspace = $ws, monitor:$mon, persistent:true
windowrule = workspace $ws silent, class:^(Spotify)$
exec-once = [workspace $ws silent] spotify

{{- end }}


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 5, 15, 15, 15

    {{- if eq .chezmoi.hostname "cordyceps" }}
    gaps_out = 1, 1, 0, 1
    {{- end }}

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(8aadf4ee) rgba(eed49fee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = true

    layout = dwindle
}


# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0


    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # hyprfocus plugin
    # animation = hyprfocusIn, 1, 0.8, default
    # animation = hyprfocusOut, 1, 2.7, default

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
  
    {{- if eq .chezmoi.hostname "amanita" }}
	  workspace_wraparound = true
    {{- end }}

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}


# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]






###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow



#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
		
		numlock_by_default = true
		repeat_delay = 240
		repeat_rate = 35

    follow_mouse = 1

    {{- if eq .chezmoi.hostname "cordyceps" }}
    sensitivity = 0.2 # -1.0 - 1.0, 0 means no modification.
		accel_profile = adaptive
		scroll_factor = 0.2
    natural_scroll = true
    {{ else }}
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
		accel_profile = flat
		scroll_factor = 1
    {{- end }}

    
}

# # https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#   gesture = 3, horizontal, workspace
# }



# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
# }

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}


# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
    mfact = 0.60
    workspace = $layoutopt
}


cursor {
	default_monitor = DP-2
}


# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

debug {
    disable_logs = false
}




